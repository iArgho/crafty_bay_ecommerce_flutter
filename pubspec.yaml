name: crafty_bay_ecommerce_flutter
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_svg: ^1.0.0
  simple_circular_progress_bar: ^1.0.2
  get: ^4.6.6
  pin_code_fields: ^8.0.1
  carousel_slider: ^5.0.0
  input_quantity: ^2.5.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/images/logo.svg
    - lib/assets/images/logo_nav.svg
